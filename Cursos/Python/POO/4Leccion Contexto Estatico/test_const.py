from constantes import CONSTANTE,Math

print(CONSTANTE)
#Sin embargo esto no es una constante como tal ya que si se puede modificar su valor
CONSTANTE = 2
#Por eso se pone esa nomenclatura para nosotros saber que no se debe de alterar su valor
#Esto es similar al concepto de encapsulacion en clases

print(Math.PI) #Asi se llamaria a una constante que de igual forma es una variable de clase